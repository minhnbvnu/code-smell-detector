function quantDef(scale){var def=q_legend_def(scale),dom=scale.domain(),data=dom.map(vg.data.ingest),width=gradientStyle.width&&gradientStyle.width.value||vg.config.legend.gradientWidth,fmt=format==null?scale.tickFormat?scale.tickFormat.apply(scale,tickArguments):String:format;var layout={name:"legend",type:scale.type,round:true,zero:false,domain:[dom[0],dom[dom.length-1]],range:[padding,width+padding]};if(scale.type==="pow")layout.exponent=scale.exponent();var tdata=(title?[title]:[]).map(vg.data.ingest);data.forEach(function(d,i){d.label=fmt(d.data);d.align=i==data.length-1?"right":i==0?"left":"center"});def.scales=[layout];def.marks[0].from=function(){return tdata};def.marks[1].from=function(){return[1]};def.marks[2].from=function(){return data};return def}