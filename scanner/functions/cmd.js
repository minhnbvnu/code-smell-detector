function cmd(layerId = 'foo', prio = 0) {
        return {
            layer: {
                id: layerId,
                protocol: 'test',
                priority: prio,
            },
            view,
        };
    }