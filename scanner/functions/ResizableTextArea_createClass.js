function ResizableTextArea_createClass(Constructor, protoProps, staticProps) {
  if (protoProps) ResizableTextArea_defineProperties(Constructor.prototype, protoProps);
  if (staticProps) ResizableTextArea_defineProperties(Constructor, staticProps);
  return Constructor;
}